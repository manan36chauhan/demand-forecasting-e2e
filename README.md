# Demand Forecasting E2E (Data + ML + API)

End-to-end project: ingestion -> transforms -> features -> training -> tracking (MLflow) -> serving (FastAPI) -> monitoring.

## Quickstart
1) Create venv and install deps:
   - source .venv/bin/activate
   - pip install -r requirements.txt
